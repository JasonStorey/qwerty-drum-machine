{
  "name": "qwerty-drum-machine",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "mkdir -p ./dist && rm -rf ./dist/*",
    "build": "npm run clean && browserify ./index.js --standalone QDM -o ./dist/qwerty-drum-machine.js"
  },
  "author": "Jason A. Storey",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^10.2.4"
  }
}
